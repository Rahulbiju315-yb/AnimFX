/**
* An interface to define a function.
* The function is evaluated at a point by calling
* the function <code>eval</code> with the x, y as 
* parameters.
* @author Rahul B.
*/
public interface lambda
{
	public double eval(double x, double y);
}